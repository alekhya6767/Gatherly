# Supabase (optional). If not set, the app uses seeded demo data.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional AI provider (later). If not set, the app will use a deterministic mock planner.
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=
